package com.ss.controller;

import javax.servlet.http.HttpSession;

import org.apache.log4j.Logger;
import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;

/**
 * 用于进行活动的发布的控制器
 * 
 * @author louis
 *
 */
@Controller
public class SaveActivityInfoController {
	
	private Logger logger = Logger.getLogger(SaveActivityInfoController.class);
	
	@RequestMapping(value="/saveactive",method=RequestMethod.POST)
	public void savePersonActivity(HttpSession session){
		
	}
}
